[Unit]
Description=Pin query app
After=pre-user-session.target
Requires=dbus.socket

[Service]
Type=simple
ExecStart=/usr/bin/qmlpinquery -systemd
Restart=always

[Install]
WantedBy=user-session.target

